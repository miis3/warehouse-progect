# Workflows

## تشغيل المطور

1. اقرأ ملفات الحقيقة وفق `AGENTS.md`.
2. ثبّت الاعتماديات: `npm ci --ignore-scripts`.
3. شغّل `npm run check` ثم `npm test`.
4. للتشغيل المتصل بقاعدة Supabase الحالية استخدم `npm start` وافتح `http://127.0.0.1:8004/`؛ أو Compose كما في `DEPLOYMENT.md`. لا تُزرع أي بيانات أو حسابات في مسار `npm start`.
5. حدّث ملفات الحقيقة و`CHANGELOG.md` قبل commit.

## تجربة محلية كاملة بلا بيانات حقيقية

1. شغّل `npm run preview:test`.
2. افتح `http://127.0.0.1:8000`.
3. العامل: أي اسم عربي ورقم جديد مثل `70001`، ثم ابحث عن `منشار` واطلب القطعة المتاحة.
4. افتح نافذة خاصة/متصفحًا آخر للإدارة وسجل بـ`preview-admin` / `Warehouse-Preview-2026!`، ثم اعتمد الصرف بالحالة المسجلة. هذه fixtures معزولة للاختبارات وليست تشغيل النظام الحقيقي.
5. حدّث «عهدتي وسجلي» للعامل، اطلب الإرجاع، واعتمده من الإدارة، ثم افتح السجل.
6. لاختبار صلاحية الأمين، أنشئ حساب «أمين مستودع» من «المستخدمون» بحساب المدير، وافتحه في تبويب مستقل. الحسابات الاختبارية وكلمات مرورها تبقى في ذاكرة Preview فقط.

كل البيانات في الذاكرة وتُحذف عند `Ctrl+C`. الحساب وكلمة المرور fixtures محلية لا تعمل على Supabase أو الإنتاج.
إذا كان Preview يعمل أصلًا وفيه بيانات تجربة تريد إبقاءها، شغّل نسخة إصلاح على منفذ آخر بتعيين `WAREHOUSE_PREVIEW_PORT` قبل `npm run preview:test`، ولا تعِد تشغيل النسخة الأصلية. اختبار المتصفح المؤرخ ونتائجه في `QA_REPORT.md`.

## العامل

1. يدخل بالاسم والرقم الوظيفي؛ الجلسة opaque وتبقى في ذاكرة الصفحة.
2. يبحث أو يفتح الخريطة/الصندوق ويحدد القطع أو الصندوق الكامل.
3. يرسل طلب استلام أو إرجاع مع `client_key` لإعادة المحاولة الآمنة.
4. لا تتغير العهدة حتى يعتمد الأمين الطلب؛ الصرف يستخدم الحالات المسجلة دون إعادة إدخالها. في الإرجاع يختار الأمين حالة عامة مع استثناءات اختيارية.
5. يرى العامل عهدته وسجله فقط ويمكنه إلغاء طلبه المعلق فقط.

## الأمين والمدير

- Supabase Auth يتحقق من كلمة المرور، ثم تنشأ جلسة مستودع منفصلة.
- الأمين يراجع الاستلام/الإرجاع، الحالات، الصيانة، الكميات، المعدات والصناديق.
- المدير يملك صلاحيات الأمين إضافة إلى إدارة العمال وحسابات الموظفين.
- المدير يحدد مدة التأخير من «إعداد التأخير». تتطبق على العهد الحالية دون إغلاقها، ويظهر تنبيه وقائمة للعهد المتأخرة. تظل الطلبات الجديدة مسموحة؛ الاعتماد يتطلب إقرارًا صريحًا بالاطلاع على تحذير العهد المتأخرة.
- أضف العدد والحالة عند إضافة المعدة. للأصناف المستوردة الناقصة فقط، استكملها من «المعدات والصناديق ← تعديل بيانات المعدة» مرة واحدة. لا توجد دورة مراجعة أو انتهاء صلاحية للعدد/الحالة.
- إضافة قطع فعلية من «إضافة قطع»، وتغيير الحالة من تفاصيل القطعة/«حالات المعدات». المتعطل والصيانة غير متاحين للصرف حتى الإصلاح.
- للصندوق الكامل زر طلب واحد واعتماد واحد، ثم عهدة مجمعة بمحتويات وقت الصرف. زر «تسليم الصندوق كاملاً» يعيد جميع القطع المتبقية المرتبطة بالصرف الأصلي.
- الإرجاع غير السليم يفتح بلاغًا آليًا في «بلاغات الصيانة» مع العامل ومستلم الإرجاع ووقته وملاحظته. «إغلاق البلاغ وإعادة المعدة سليمة» يغلق التاريخ دون حذفه ويعيد التوفر.
- السجل تلقائي للقراءة والبحث ونوع العملية والتاريخ. حذف قطعة تشغيليًا هو استبعاد موثق يحفظ تاريخها، وليس حذف صفوف.
- تعطيل الحساب يبطل جلساته، وتغيير كلمة المرور يبطل الجلسات الأخرى.
- إنشاء المدير الأول يحتاج setup token أحادي الاستخدام مخزنًا كتجزئة.

## تغييرات قاعدة البيانات

1. لا تعدّل migrations السابقة.
2. اكتشف CLI عبر `supabase --help` وأنشئ الملف بـ`supabase migration new`.
3. جرّب على قاعدة محلية/معزولة، وشغّل الاختبارات وadvisors.
4. راجع الصلاحيات وRLS و`SECURITY DEFINER` والفهارس.
5. تطبيق remote والنشر خطوتان منفصلتان تحتاجان بيانات اعتماد وتفويضًا؛ ليستا جزءًا من CI الحالي.

## CI

Workflow الاختبارات يعمل عند push لأي فرع وpull request إلى `main`:

- تثبيت npm من lockfile.
- فحص JavaScript والأصول وحدود الأسرار.
- جميع اختبارات Node/PGlite/واجهة على Windows وLinux، بما فيها قواعد التأخير والصيانة والصندوق وproxy Supabase المحلي الذي لا يزرع بيانات.
- فحص Compose، بناء صورة Caddy، ثم smoke test للصحة والواجهة وإعداد same-origin.

Workflow GitHub Pages الحالي منفصل، ومقيد بـpush إلى `main`. هذه المهمة لم تشغله ولم تغيّر وجهة النشر.

## إصدار حي لاحق

يتطلب قرارًا مستقلًا: مراجعة بصرية، backup، اختبار staging، advisors، تطبيق migration، نشر Edge Function، ضبط origins/DNS، ثم نشر واجهة. عند الفشل لا تُرجع schema بشكل هدّام؛ أضف migration تصحيحيًا وأعد صورة التطبيق السابقة.
