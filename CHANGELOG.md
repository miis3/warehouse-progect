# Changelog

كل التغييرات المهمة في هذا المشروع موثقة هنا. التواريخ بصيغة ISO.

## 2026-09-20 — Automatic custody, overdue and maintenance workflows

- أزيلت واجهة مراجعة العدد والحالة؛ أصبحت بيانات أولية عند إضافة المعدة، مع استكمال أولي للأصناف القديمة دون تخمين أو تعديل مصادرها.
- الصرف آلي بالاعتماد بالحالة الحالية، وإرجاع الصندوق آلي مع حفظ محتوياته الأصلية؛ العهد مجمعة حسب الصندوق.
- إعداد مدير لعدد أيام التأخير، تنبيهات الإدارة، وتحذير قبل صرف طلب عامل متأخر دون رفض تلقائي.
- بلاغات صيانة تلقائية للإرجاع المتعطل مرتبطة بالقطعة والعامل والعهدة والطلب والمستلم، وإغلاق موثق يعيد التوفر.
- استبعاد إداري آمن للقطع دون محو التاريخ، وسجل تلقائي قابل للبحث والتصفية وعرض snapshots.
- Migration additive `20260920032912_warehouse_workflows.sql` وفهارس الإنتاج طُبقا على Supabase staging؛ RLS وRPC الخاصة والبيانات الأصلية محفوظة. Edge الإصدار 2، لا نشر حي.
- `npm start` لتشغيل نسخة Windows متصلة بـSupabase دون حسابات أو بيانات وهمية، وتصحيح مخزن شهادات Windows دون تعطيل TLS.
- إصلاح Docker gateway 401 بإضافة anon JWT العام، مع إبقاء مفتاح الامتياز داخل Supabase Edge فقط.
- CI يختبر Windows وLinux بالإضافة إلى حاوية Caddy. مجموعة التحقق أصبحت 27 اختبارًا؛ اختبار المتصفح أتم قطعة/صيانة وصندوقًا بست قطع وإعادة الدخول والسجل دون Console errors.
- مصحح بصمة مصدر المخزون عبر LF/CRLF بعد اكتشاف المشكلة في Windows CI، دون تغيير المصدر أو المعرفات أو البصمة الأصلية المعتمدة.
- الدخول التشغيلي الحقيقي ينتظر كلمة مرور حساب `admin` الموجودة؛ لم نغيرها ولم نملأ كميات أو حالات غير معروفة. راجع `QA_WORKFLOWS_REPORT.md`.

## 2026-09-17 — Production-ready Windows branch

### Added

- `AGENTS.md` وعائلة ملفات الحقيقة للتشغيل والصيانة والتسليم.
- صورة Caddy ثابتة و`compose.yaml` و`.env.example` لتشغيل Windows بأمر واحد.
- same-origin reverse proxy إلى Supabase، healthcheck، ضغط، رؤوس أمنية، ودعم HTTPS الذاتي.
- اختبارات بنية الإنتاج وRLS/فهارس المفاتيح الأجنبية.
- migration additive لفهارس `loans.unit_id`, sessions actor, وrate-limit retention.
- CI لبناء الحاوية وفحصها دون نشر.

### Changed

- فحص المصدر يغطي إعداد Docker ويمنع مؤشرات مفاتيح الامتياز في browser config.
- Workflow الاختبارات يعمل على كل الفروع وpull requests إلى `main`.

### Preserved

- الواجهة RTL والخريطة والصناديق والبحث والصور والباركود وجميع البيانات المصدرية.
- Supabase migrations السابقة، RLS، صلاحيات RPC، وسلوك Edge Function.
- Workflow النشر الحي المقيد بـ`main`; لم يحدث نشر.

### Architecture decision

- لم يُعتمد Next.js لأن فائدته الحالية لا تبرر إعادة كتابة واجهة مستقرة.
- أُضيف Caddy لاستخدام فعلي في التقديم وreverse proxy والأمان وHTTPS، لا كاعتماد شكلي.

### Verification

- GitHub Actions run `35284438985` نجح في job الاختبارات وjob بناء/فحص حاوية الإنتاج.

## 2026-09-18 — Isolated hands-on preview

### Added

- `npm run preview:test` لتشغيل الواجهة الفعلية وEdge handler فوق قاعدة PGlite مؤقتة على `127.0.0.1`.
- حساب مدير اختبار محلي وقطعة متاحة مهيأة تلقائيًا لتجربة الاستلام والإرجاع.
- اختبار HTTP متكامل يثبت دورة العامل والأمين والعهدة والإرجاع والسجل.

### Safety

- لا Supabase credentials ولا اتصالات بعيدة ولا أثر على `main` أو الموقع الحي أو البيانات الحقيقية.

## 2026-09-20 — Browser QA and preview staff fix

- اختُبرت فعليًا عبر المتصفح رحلة العامل للقطعة والصندوق الكامل، اعتماد المدير والأمين، الرفض، الإرجاع، العهدة، الصيانة، السجل، إعادة الدخول، ومنع صرف الصندوق المكرر.
- أُصلح محاكي هوية Preview ليتيح إنشاء حساب أمين اختبار وتغيير كلمة مروره دون الاتصال بخدمة Supabase بعيدة. الإنتاج لم يتغير.
- أُضيفت اختبارات HTTP/SQL للصلاحيات، منع الصرف المكرر، كميات العهدة، والسجل بعد الإرجاع؛ التقرير والصور في `QA_REPORT.md`.
- أُبقي خادم التجربة القديم على 8000 دون إعادة تشغيل حفاظًا على بياناته المؤقتة، وشُغّلت النسخة المصححة على منفذ مستقل.
