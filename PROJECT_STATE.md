# حالة المشروع

آخر تحديث: 2026-09-20

## النسخة الحالية

- الفرع: `codex/production-ready-windows`؛ لا تعديل أو دمج في `main` ولا نشر للواجهة الحية.
- التصميم العربي RTL والخريطة والصناديق والصور والبحث وQR محفوظة، بلا إعادة بناء أو Next.js.
- `npm start` يشغل نسخة Windows على `http://127.0.0.1:8004/` متصلة بقاعدة Supabase الحالية، دون تهيئة حسابات أو مخزون وهمي. يتطلب Node 24 ويستخدم مخزن شهادات النظام مع التحقق من TLS.
- مشروع Supabase المتصل هو `warehouse-management-staging` (`gpiltdkbrryfhwqisioe`). تم تطبيق فهارس الإنتاج وmigration سير العمل عليه فقط، وتحديث Edge Function إلى النسخة 2 مع `verify_jwt=true`.
- قبل/بعد الترقية: 258 بند مخزون، 0 قطع تشغيلية، 0 عمال، 0 عهد، 1 حدث سجل سابق. لم تُفترض كميات أو حالات ولم تُنشأ بيانات تجريبية في Supabase.
- الحساب الإداري الموجود: `admin`. كلمة مروره لم تُقرأ أو تُغيّر. الدخول الحقيقي يحتاج صاحب الحساب.
- الأصناف المستوردة غير مكتملة المخزون التشغيلي: يُدخل الأمين العدد والحالة الأوليين من «تعديل بيانات المعدة» مرة واحدة. لا توجد مراجعة دورية أو إعادة تأكيد لاحقة.
- المعدات الجديدة تتطلب العدد والحالة ضمن الإضافة نفسها. الصرف والإرجاع يعتمدان تلقائيًا مع العهد والسجل، والصندوق يظهر مجموعة تحتفظ بمحتويات وقت الصرف.
- إعداد التأخير في قاعدة البيانات يحدده المدير دون قيمة مفترضة؛ العامل المتأخر يستطيع الطلب، والإدارة تطلع على تحذير قبل الموافقة.
- الإرجاع غير السليم ينشئ بلاغ صيانة مرتبطًا بالعامل والقطعة والعهدة والطلب ومستلم الإرجاع. إغلاقه يعيد المعدة سليمة ومتاحة.
- الاستبعاد الإداري soft-retirement فقط؛ لا حذف لتاريخ القطعة.
- السجل تلقائي ثابت قابل للبحث والتصفية. الصلاحيات مفروضة داخل PostgreSQL.

## التحقق

- Baseline لهذه المهمة عند `bdb2152`: `npm ci --ignore-scripts` و`npm run check` ناجحان، `npm test` 19/19.
- بعد التعديلات: 27/27 اختبار ناجح؛ تشمل الوظائف السابقة والمخطط الجديد وWindows local proxy وFK/RLS ورفض تجاوز الملكية والصرف المكرر والتأخير والصيانة وإرجاع محتويات الصندوق الأصلية.
- اختبار متصفح فعلي على خادم تحقق منفصل 8003: رحلة قطعة مع إرجاع متعطل وصيانة وإصلاح، وصندوق بست قطع مع إعادة دخول وإرجاع جماعي سليم وسجل. لا أخطاء/تحذيرات Console في جلستي العامل والمدير.
- اختبار الاتصال الفعلي على 8004: تحميل الصندوق الأصلي من Supabase وظهور الصور والمحتويات نجح؛ أصلح خطأ شهادة Node باستخدام `--use-system-ca` دون تعطيل TLS.
- فحص Supabase الفعلي: RLS مفعلة على الجداول الجديدة، لا execute لـanon على API ولا لـservice_role على core الخاص.
- advisors: لا تحذيرات RLS أو فهارس FK ناقصة. تحذير Auth الوحيد: حماية كلمات المرور المسرّبة غير مفعلة. فهارس غير مستخدمة INFO متوقعة لقاعدة بلا حركة؛ لم نحذفها.
- اكتُشف أن proxy Docker السابق يعيد 401 لأن JWT gateway لا يقبل apikey وحده. أضيف `SUPABASE_ANON_KEY` العام لـAuthorization؛ لا يرسل publishable غير JWT كـBearer.
- تشغيل CI الأول `35488002400` نجح في Linux والحاوية، وكشف اختلاف بصمة مصدر المخزون على Windows بسبب CRLF. صحح احتساب SHA256 إلى LF canonical مع اختبار اختلاف نهايات الأسطر وكشف تعديل المحتوى الحقيقي؛ لم يتغير المصدر أو manifest أو بيانات المستودع.
- Docker غير متوفر على Windows المحلي. CI على الفرع يختبر Windows وLinux ويبني حاوية Caddy ويفحصها دون نشر. تُسجّل نتيجة آخر تشغيل في تقرير التسليم.
- التقرير التفصيلي: `QA_WORKFLOWS_REPORT.md`. تقرير `QA_REPORT.md` يخص التحقق السابق قبل هذه الترقية.

## حدود الجاهزية

- اختبار التعديل الفعلي في Supabase بحساب المدير لم يكتمل لعدم توفر كلمة مروره. لا ينبغي إنشاء هوية أو بيانات وهمية لتجاوز ذلك.
- بيانات المخزون التشغيلية الأولية الفعلية ومدة التأخير قرارا مسؤول المستودع؛ لم نخمنهما.
- PGlite يعالج PostgreSQL بمعاملات حقيقية لكنه ليس اختبار ضغط متعدد اتصالات على الخادم البعيد. يلزم load test مستقل قبل توسع الاستخدام.
- دخول العامل بالاسم والرقم الوظيفي ليس إثبات هوية قويًا؛ SSO/OTP يحتاج قرارًا منفصلًا.
- حماية كلمات المرور المسرّبة تحتاج مراجعة خطة Supabase؛ لم نفعّل خدمة مدفوعة.
- خوادم التجربة السابقة 8000 و8002 لم تُعد تشغيلها لحفظ بياناتها المؤقتة؛ ليست نسخة Supabase التشغيلية الجديدة. استخدم 8004.
